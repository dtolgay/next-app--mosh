'use client'
import React from 'react'

const AddToCard = () => {
    return (
        <button className='btn btn-primary' onClick={() => console.log('Clicked')}>Add 2 card</button>
    )
}

export default AddToCard
